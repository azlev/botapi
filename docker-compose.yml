version: '3'

services:
  db:
    image: postgres
  botapi:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/botapi
    ports:
      - "8000:8000"
    depends_on:
      - db
